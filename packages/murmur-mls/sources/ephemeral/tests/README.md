# Ephemeral frame tests

Coverage for the round trip between two members of one epoch, per-sender
ordering and replay rejection, forgery by an insider holding the same exporter
secret, frames from another epoch, self-echo and unknown-leaf drops, the
bounded per-sender stream table, payload bounds, malformed input, and the
independence of key material derived from different epoch secrets.
