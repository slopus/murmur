# CLI tests

Command tests verify the non-interactive JSON surface independently from the
filesystem/bootstrap entry point. Bootstrap tests pin the public default relay
and the argument-over-environment precedence used to override it.
