# Messaging tests

Application-message codecs and encrypted-file confidentiality, integrity, size,
and filename rules, plus direct-message sender/recipient binding and strict wire
validation. Durable tests cover same-content replay, conflicting reuse of a
signed message ID, and restart-safe acceptance state.
