# Direct-chat implementation

Strict local codecs retain canonical send decisions and exact signed relay
events for crash-safe retry. These are local persistence formats, not new relay
or message wire versions.
