# Direct-chat tests

Observable engine tests cover text send/retry, authorization, permanent history,
self-copy separation, replay/collision quarantine, removal, resets, and atomic
consumer callbacks over in-memory relays.
