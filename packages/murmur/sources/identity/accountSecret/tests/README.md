# Account-secret tests

These tests cover identity-root round trips, both-factor authentication,
ciphertext integrity, strict blob versions, canonical serialization, and local
password changes. They use the real Noble scrypt, HKDF, and AES-GCM
implementations.
