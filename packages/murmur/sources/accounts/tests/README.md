# Account tests

Tests exercise strict roster and mutation codecs. Integration coverage restores
one account on a second store, verifies ordinary inbox notification, converges
the new MLS leaf, and removes the device.
