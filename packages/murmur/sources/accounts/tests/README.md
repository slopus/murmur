# Account tests

Tests exercise roster signature validation, replay and parent binding,
deterministic fork resolution, device credentials, provisioning expiry,
key-possession proofs, transcript binding, and encrypted account-root transfer.
Integration coverage creates ordinary sessions from bare KeyPackages before
linking and revoking account devices.
