# Contact tests

Top-level contact tests belong here once the public client handshake is wired
into the session engine. They exercise invitation-to-contact behavior through a
real local relay and application store.

Low-level codec and record tests live under `impl/tests`.
