# Service tests

Public service registration, descriptor copying, and durable routing tests.

```text
service API -> callback routing -> durable owner record
```

Session fixtures use bare KeyPackage admission.
