# Relay sessions

This domain defines the application-authenticated temporary routing ticket used
by the additive WebSocket transport. The application authorizes the account to
device binding; the relay token remains bound to that device's public key.
