# Cloudflare adapter tests

The in-memory Durable Object harness checks strict internal JSON boundaries,
manifest-first sequencing, partial fanout retry, alarms, and idempotent
per-device inbox insertion.
