# GitHub automation

```text
version tag
    |
    `-- workflows/release.yml
```

GitHub-specific automation lives in `workflows`. Product intent remains in the
repository's master plans rather than in workflow configuration.
